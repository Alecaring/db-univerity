
-- -------------------------------------------------
SELECT name, surname, date_of_birth
FROM `students` 
WHERE `date_of_birth` LIKE "1990%"
-- -------------------------------------------------

-- -------------------------------------------------
SELECT name, cfu
FROM `courses` 
WHERE `cfu` > "10"
-- -------------------------------------------------

-- -------------------------------------------------
SELECT 
  `name`, `surname`, `date_of_birth`
FROM 
  `students`
WHERE 
  `date_of_birth` > '1994';
-- -------------------------------------------------

-- -------------------------------------------------
SELECT 
  `name`, `period`, `year`
FROM 
  `courses`
WHERE 
  `period` = "I semestre" AND `year` = "1" ;
-- -------------------------------------------------

-- -------------------------------------------------
SELECT 
  `date`, `hour`
FROM 
  `exams`
WHERE 
  `date` = "2020-06-20" AND `hour` > "14:00:00" ;
-- -------------------------------------------------

-- -------------------------------------------------
SELECT `name`, `level`
FROM `degrees`
WHERE `level`= "magistrale"
-- -------------------------------------------------

-- -------------------------------------------------
SELECT DISTINCT `name`
FROM `departments`;
-- -------------------------------------------------

-- -------------------------------------------------
SELECT DISTINCT `phone`, `name`, `surname`
FROM `teachers`
WHERE `phone` IS NULL
-- -------------------------------------------------




